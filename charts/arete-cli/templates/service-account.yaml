
---
kind: ServiceAccount
apiVersion: v1
metadata:
  name: {{ .Values.cnscp.serviceAccountName }}
  namespace: {{ .Release.Namespace }}
{{- if .Values.cnscp.imagePullSecrets }}
imagePullSecrets:
  {{- range .Values.cnscp.imagePullSecrets }}
  - name: {{ . }}
  {{- end }}
{{- end }}
