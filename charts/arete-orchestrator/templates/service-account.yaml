
---
kind: ServiceAccount
apiVersion: v1
metadata:
  name: {{ .Values.orchestrator.serviceAccountName }}
{{- if .Values.orchestrator.imagePullSecrets }}
imagePullSecrets:
  {{- range .Values.orchestrator.imagePullSecrets }}
  - name: {{ . }}
  {{- end }}
{{- end }}
