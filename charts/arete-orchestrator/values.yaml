---
orchestrator:
  name: orchestrator
  serviceAccountName: orchestrator
  imageRepository: us-central1-docker.pkg.dev/padi-80910/cnscp-preprod/cns-orchestrator
  imageTag: staging
  pullPolicy: IfNotPresent
  imagePullSecrets:
    - artifact-registry-secret
    - padi-artifact-hub
  credentialsSecretRef: arete-etcd-user-credentials
  command: ["node", "index", "--debug"]
  replicas: 1
  config:
    - name: CNS_HOST
      value: "http://arete-etcd"
    - name: CNS_PORT
      value: 2379
    - name: CNS_PROFILES
      value: "https://cp.padi.io/profiles"

  # Additional Environment variables can go here
  #
  # env:
  #   - name: 
  #     value: 

  # If rollPods is set to True, it will create a randAlphaNum annotation
  # which is used to roll the pods on each deploy
  #
  # rollPods: false
